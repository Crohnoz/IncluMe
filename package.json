{
  "name": "inclume-accessibility-tests",
  "private": true,
  "version": "5.0.0",
  "scripts": {
    "test:browser": "playwright test",
    "test:admin-browser": "playwright test --config=playwright.admin.config.js"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "^1.54.1"
  }
}
