services:
  - type: web
    name: inclume-app
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt && python manage.py check --deploy && python manage.py collectstatic --noinput && python manage.py migrate
    startCommand: gunicorn --workers 2 --threads 2 --timeout 60 --access-logfile - --error-logfile - IncluMe.wsgi:application
    healthCheckPath: /health/
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.4
      - key: DEBUG
        value: "false"
      - key: SECRET_KEY
        generateValue: true
      - key: ALLOWED_HOSTS
        value: inclume-app.onrender.com
      - key: CSRF_TRUSTED_ORIGINS
        value: https://inclume-app.onrender.com
      - key: SECURE_HSTS_SECONDS
        value: "31536000"
      - key: DATABASE_URL
        fromDatabase:
          name: inclume-db
          property: connectionString

databases:
  - name: inclume-db
    plan: free
